# Sanity CMS Configuration
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=your-api-token-with-write-access

# Site Configuration (Optional)
# Set this for production custom domain
# If not set, will auto-detect Vercel URL or use localhost for development
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# Note: VERCEL_URL is automatically set by Vercel during deployment
# You don't need to set it manually
